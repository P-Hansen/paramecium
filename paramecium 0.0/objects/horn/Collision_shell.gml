/// @description Insert description here
// You can write your code in this editor

instance_destroy(self);
other.image_xscale = clamp(other.image_xscale - 0.5, 0, 1);
other.image_yscale = clamp(other.image_yscale - 0.5, 0, 1);