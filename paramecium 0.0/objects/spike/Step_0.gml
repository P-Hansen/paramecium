/// @description Insert description here
// You can write your code in this editor
image_angle = direction;
image_xscale = clamp(image_xscale + 0.01, 0, maxSize);
image_yscale = clamp(image_yscale + 0.01, 0, maxSize);